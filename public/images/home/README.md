# Homepage-foto's

| Bestand | Gebruik op `/` |
|---------|----------------|
| `img1.png` | Hero (rechterkolom) |
| `img2.png` | Sfeerbeeld onder sectie "Hoe wij werken" |

Beide bestanden zijn door Marten aangeleverd. Vervang ze hier voor livegang indien je `.webp` of `.jpg` wilt gebruiken — pas dan de `src` in `Hero.tsx` en `HomeMidImage.tsx` aan.
